# What the application does

Being rewritten.

A description of the analysis pipeline stage by stage belongs here. The
previous text has not been checked against the current implementation, and an
unverified specification is worse than none — so it is withheld until it can
be confirmed against the code.

In the meantime the code itself is the specification, and the test suite states
most of it explicitly: each test's docstring says what behaviour it protects
and, usually, what went wrong once to make it necessary.
